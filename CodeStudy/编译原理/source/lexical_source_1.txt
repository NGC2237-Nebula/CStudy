const a = 10;
var   b, c;
procedure fun1;
    if a <= 10 then
        begin
            c := b + a;
        end;
begin
    readb);
    while b # 0 do
        begin
            call fun1;
            write(2 * c);
            read(b);
        end
end.